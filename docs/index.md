# openapi-to-human-reference

This repo converts an OpenAPI YAML/JSON spec into a clean, human-readable API reference in Markdown.

## What this site contains
- A published example of the generated output
- A simple baseline you can extend (styles, templates, richer schema support)

Next: add a GitHub Pages workflow so this site is published automatically.
